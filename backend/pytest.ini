[pytest]
# 1. Add current directory to Python Path so 'app' module is found
pythonpath = .

# 2. Configure AsyncIO so you don't need strict decorators everywhere
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function